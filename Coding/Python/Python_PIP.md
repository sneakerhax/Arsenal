# Python - PIP

```
python3 -m pip list
```

List pip modules (updated version)

```
python3 -m pip install --upgrade pip
```

Update pip version. --user optional to update in local directory (does not require root or sudo)
